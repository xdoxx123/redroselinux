# This file is deprecated. The main Makefile now handles Fedora automatically.
# Run 'make' instead of 'make -f Makefile-fedora'.

FS_DIR = filesystem
INITRAMFS_DIR = initramfs
OUTPUT_DIR = .

INITRAMFS_CPIO = $(OUTPUT_DIR)/initramfs.cpio
INITRAMFS_GZ = $(OUTPUT_DIR)/initramfs.cpio.gz
ISO = $(OUTPUT_DIR)/redrose_linux.iso

all:
	@echo "  DEPRECATED: Makefile-fedora is no longer needed."
	@echo "  The main Makefile now detects Fedora automatically."
	@echo ""
	@echo "  Run 'make' instead of 'make -f Makefile-fedora'"
	@echo ""
	@echo "  To run with the main Makefile:"
	@echo "    $$ make"
	@false

help: all

.PHONY: all help
